import { defineConfig } from 'vite';

export default defineConfig({
  // No specific configuration needed for now,
  // but having this file can sometimes help Vite
  // correctly identify the project root and asset handling.
});